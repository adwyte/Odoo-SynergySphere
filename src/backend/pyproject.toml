[project]
name = "synergysphere-backend"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "sqlalchemy>=2.0",
  "psycopg[binary,pool]>=3.2",
  "alembic",
  "pydantic>=2",
  "python-multipart",
]
